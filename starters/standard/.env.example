# WorkOS Configuration
# Get these from https://dashboard.workos.com/
WORKOS_CLIENT_ID=client_xxx
WORKOS_API_KEY=sk_xxx

# Redirect URI - must match what's configured in WorkOS dashboard
WORKOS_REDIRECT_URI=http://localhost:3000/auth/callback

# Cookie password for encrypting sessions (32+ characters)
# Generate with: openssl rand -base64 32
WORKOS_COOKIE_PASSWORD=your-32-character-or-longer-password-here

# Convex (already configured)
# VITE_CONVEX_URL=https://xxx.convex.cloud
